upstream svp_api_gateway {
    server api-gateway:8080;
}

server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://svp_api_gateway;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_read_timeout 300s;
        client_max_body_size 200m;
    }

    # 头像：Nginx 直读共享卷 media_data（只读挂载 /data/media）
    location /media/avatars/ {
        alias /data/media/avatars/;
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
        access_log off;
    }

    # 未审核源片禁止直出
    location ^~ /media/uploads/ {
        return 403;
    }

    # 转码后 HLS/封面：Nginx 直出
    location /media/transcoded/ {
        alias /data/media/transcoded/;
        expires 1h;
        add_header Cache-Control "public";
        sendfile on;
        tcp_nopush on;
        types {
            application/vnd.apple.mpegurl m3u8;
            video/mp2t ts;
            video/mp4 mp4;
            image/jpeg jpg jpeg;
            image/png png;
            image/webp webp;
        }
    }

    # 受保护源片走 api-gateway 鉴权
    location /api/v1/media/uploads/ {
        proxy_pass http://svp_api_gateway;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Authorization $http_authorization;
    }
}
