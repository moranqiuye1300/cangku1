# 短视频平台项目 — 面试面经（中文）

> 基于仓库 **`short-video-platform`** 真实实现整理，与代码、`后端架构与流程说明.md`、`能力对照评估.md` 一致。  
> **原则：能讲清的讲透；没做的诚实说「规划/演进」，避免与代码不符被追问穿帮。**

---

## 使用说明

| 标记 | 含义 |
|------|------|
| ✅ | 代码里已实现，可结合文件路径细讲 |
| 🟡 | 部分实现或简化版 |
| 📌 | 未实现，用「若上线我会…」回答 |

---

## 一、30 秒 / 1 分钟项目介绍（必背）

### 30 秒版

我做了一个 **短视频播放平台** 后端：对外 **Gin HTTP 网关**，对内 **gRPC 微服务**（用户 MySQL、视频 MongoDB），视频走 **双阶段人工审核**，源片通过后 **Kafka 异步 FFmpeg 转 HLS**，发布后才进 Feed；配套 **Elasticsearch 搜索**、点赞收藏弹幕、基于标签权重的 **简单个性化推荐**，Docker Compose 一键起全栈。

### 1 分钟版（STAR）

- **背景**：课程/个人项目，模拟「上传 → 审核 → 转码 → 发布 → 竖滑播放」完整链路。  
- **任务**：状态机清晰、转码不阻塞上传、公开面只暴露已发布内容。  
- **行动**：拆 api-gateway / user-service / video-service / transcode-worker；Kafka 解耦转码；gRPC 拦截器做 JWT + internal key + 审核员角色；源片路径鉴权、转码资源分级访问。  
- **结果**：主链路可演示；种子数据 + 真实转码产物可播放；文档与评估对齐代码。（**未做压测宣称 200 QPS 等，除非你真压过。**）

---

## 二、架构与设计（高频）

### Q1：整体架构怎么画的？为什么用网关 + gRPC？

**答（✅）：**

- 浏览器只调 **api-gateway**（REST），网关不碰业务库，只做协议转换、上传落盘、JWT、限流。  
- **user-service**、**video-service** 走 gRPC，契约在 `proto/`，生成代码在 `gen/`。  
- **transcode-worker** 无 HTTP，只消费 Kafka，避免长耗时 FFmpeg 拖垮 API 进程。  

**好处：** 边界清晰、转码可独立扩容、对内强类型。  
**代价：** 运维组件多（Mongo/Kafka/ES），本地靠 Compose。

📌 未做：K8s、服务注册发现、链路追踪。

---

### Q2：服务怎么拆的？各管什么？

| 服务 | 端口 | 存储 | 职责 |
|------|------|------|------|
| api-gateway | 8080 | 本地 `MEDIA_ROOT` | HTTP、上传写盘、媒体访问 |
| user-service | 50051 | MySQL | 注册登录、OAuth、角色 |
| video-service | 50052 | Mongo + ES + Kafka 生产 | 视频状态机、互动、推荐、搜索、发转码任务 |
| transcode-worker | — | 共享卷 | Kafka 消费、FFmpeg、回写 gRPC |
| ai-service | 8000 | 无 | 关键词标签、RAG 问答代理 |

**user-service 为何调 video-service？** 用户主页视频列表要带「本人看全状态 / 他人只看 ready」逻辑，由 video 域统一判断。

---

### Q3：一次上传视频的完整链路？

**答（✅，按顺序背）：**

1. `POST /api/v1/videos/upload`（JWT）→ 网关 `FormFile`，校验 **≤200MB**。  
2. `SaveUpload` → `uploads/u{userId}/{tempId}/source.ext` 写共享卷。  
3. gRPC `CreateVideo` → Mongo 插入，`status = pending_source_review`，**不发 Kafka**。  
4. 审核员 `approve-source` → CAS `pending_source_review` → `transcoding` → `PublishTranscode`。  
5. worker 消费 → FFmpeg → `UpdateTranscodeResult` → `pending_final_review` + `play_urls`。  
6. 审核员 `approve-publish` → `ready` → 写 ES 索引。  
7. Feed / 搜索 / 点赞仅对 `ready` 开放。

**追问：为什么上传不直接转码？** 先审源片，避免违规内容进转码流水线、浪费算力。

---

### Q4：视频状态有哪些？怎么防止状态被乱改？

| 状态 | 含义 |
|------|------|
| `pending_source_review` | 待源片审核 |
| `transcoding` | 已发 Kafka |
| `pending_final_review` | 转码完成待发布审 |
| `ready` | 已发布 |
| `failed` | 转码失败 |

**防护（✅）：**

- 源片通过用 `UpdateStatusIf`（CAS），防止重复点通过重复发 Kafka。  
- Worker **不能** 把状态直接改成 `ready`，只能 `pending_final_review` 或 `failed`。  
- `UpdateTranscodeResult` 仅接受 **internal gRPC key**（worker 专用）。

---

### Q5：Kafka 在项目中怎么用？失败怎么办？

**答（✅）：**

- Topic：`video.transcode`，消息体 `pkg/events.TranscodeTask`（video_id、source_path、标题等）。  
- 分区键：`video_id`，同视频任务进同一分区便于有序。  
- 消费者：`transcode-worker`，`TRANSCODE_CONCURRENCY` 控制并发。  
- **失败不 `MarkMessage`** → 消息可重投（需注意 FFmpeg 重复执行会覆盖输出目录）。

📌 未做：死信队列、消费延迟监控、幂等 idempotency key。

**若被问「如何保证 exactly-once」：** 诚实说当前 at-least-once + 状态机校验；生产会加「转码任务版本号」或对象存储 etag。

---

### Q6：转码参数？为什么 HLS 不是 MP4？

**答（✅）：**

- `ffprobe` 取时长；第 1 秒截封面；**720p / 1080p** 两档 HLS（H.264 + AAC，4s 分片）。  
- 输出路径：`/media/transcoded/{videoId}/{quality}/index.m3u8`。  

**为什么 HLS：** 多码率切换、CDN 友好；前端用 hls.js。  
📌 未做：480p、MP4 渐进、水印、master playlist 自动 ABR。

---

## 三、存储与数据库

### Q7：为什么用户 MySQL、视频 Mongo？

**答：**

- **用户**：强一致、关系简单、角色字段，GORM 迁移方便。  
- **视频 + 互动**：文档模型灵活（`play_urls` map、tags 数组、软删字段）；评论/点赞/弹幕高写入适合嵌套集合。  

📌 未做分库分表。深分页仍是 offset，会提 cursor 方案。

---

### Q8：Mongo 建了哪些索引？为什么？

**答（✅）：**

- `video_id` 唯一  
- `user_id + created_at`（个人页列表）  
- `status + created_at`（审核列表、ready 池）  
- 点赞/收藏 `(video_id, user_id)` **唯一** → 防重复点赞  

---

### Q9：Elasticsearch 怎么用？

**答（✅）：**

- 仅 **`ready`** 视频入索引；`SearchVideos` 先 ES 拿 id 再 Mongo 补全。  
- 发布审核通过时 `indexVideo`；启动时可 `ReindexSearch`。  

📌 未做：IK 分词调优、热搜榜、搜索建议。

---

### Q10：文件存在哪？为什么 Compose 里还有 MinIO？

**答（✅）：**

- **实际上传/转码走本地卷 `media_data`**，网关与 worker 共享。  
- `pkg/objectstore` 有 MinIO 抽象，**主路径未接入**。  

📌 面试可说：「当前作业用共享卷简化；生产会切 OSS + 预签名分片上传。」

---

## 四、安全与鉴权（必问）

### Q11：JWT 怎么传？gRPC 怎么鉴权？

**答（✅）：**

- 登录返回 JWT，网关中间件解析 `user_id / role`。  
- 调 gRPC 时 `pkg/auth/metadata.go`：`Authorization: Bearer` + **`x-internal-key`**（防外网直连微服务）。  
- video-service 拦截器分四类：**Public / Internal / JWT / Admin / Reviewer**（见 `pkg/auth/grpc_server.go`）。  
- user-service：**默认都要 internal key**；admin 接口额外要 admin 角色。

---

### Q12：源片和成片权限有何不同？

**答（✅）：**

- **源片** `GET /api/v1/media/uploads/*`：JWT + 本人或 reviewer/admin（`CanAccessUploadPath`）。  
- **转码 HLS** `GET /media/transcoded/*`：**当前公开**（知道 URL 可拉）。  

📌 诚实缺口：无签名 URL、无防盗链。  
**生产答法：** 短时效 presigned URL + CDN Referer 校验 + 播放 token。

---

### Q13：怎么防止用户伪造 source_path 上传别人的文件？

**答（✅）：** `ValidateSourcePathForUser` 要求路径必须在 `uploads/u{自己的userId}/` 下，且禁止 `..`。

---

## 五、业务功能

### Q14：推荐 Feed 怎么做？

**答（🟡）：**

- 登录用户有标签权重时：拉最多 100 条 `ready` → 按标签分 + 7 天内新片加分 + 小随机 → 内存排序分页。  
- 否则按时间倒序列表。  
- 点赞 +2 / 取消 -2，收藏 ±3 更新权重。  

📌 无播放时长埋点、无协同过滤、无 Redis 缓存 Feed。  
**扩展答：** 离线算用户 embedding、Redis ZSET 存 Feed 快照、冷启动池。

---

### Q15：点赞会不会重复？高并发怎么办？

**答（✅）：**

- Mongo 唯一索引 `(video_id, user_id)` + Toggle 语义。  
- **同步写**，无 Kafka 削峰。  

📌 可说：生产用 Redis 计数 + 异步落库、或写扩散/读扩散。

---

### Q16：双阶段审核和「先审后转」？

**答（✅）：** 先 **源片快审**（看原文件是否违规/质量），再转码，再 **发布精审**（看封面、标题、成片）。比「先转码再审」省算力。  
ai-service 只有**关键词标签**，**无**涉黄暴恐机审（📌 诚实）。

---

### Q17：ai-service 做了什么？

**答（✅）：**

- `POST /tags/generate`：标题描述关键词 → 标签（worker 转码后合并进 Mongo）。  
- `POST /rag/ask`：调网关搜索接口拼回答（教学向 RAG MVP）。  

📌 非 LangChain 向量库全链路，别夸大。

---

## 六、Golang 与工程（结合项目）

### Q18：context 在项目里怎么用？

网关 `handler.ctx(c)` 把 Bearer 放进 context；gRPC 客户端拦截器 `OutgoingContext` 带 metadata；worker 用 `signal.NotifyContext` 优雅退出。

### Q19：为什么转码 worker 用 errgroup？

`worker.Run` 里一个 goroutine 跑 `Consume` 循环，另一个消费 `Errors()` 通道，任一退出可取消整体。

### Q20：并发控制转码怎么做？

`consumerGroupHandler` 里 `sem` channel，容量 `TRANSCODE_CONCURRENCY`，防止 FFmpeg 吃满 CPU。

### Q21：gRPC 错误怎么传到 HTTP？

网关 `writeGRPCError` 把 `status.Code` 映射 404/409/500 等，统一 JSON 包装（`internal/response`）。

### Q22：如果线上 panic 怎么办？

Gin 默认 Recovery；gRPC 可加 recovery 拦截器（📌 可答「计划加」）。

---

## 七、性能、运维、压测（小心回答）

### Q22：QPS、P99 多少？

**诚实版：** 作业项目**未做正式压测报告**；瓶颈可能在 Mongo 全表扫推荐池、ES、磁盘 IO 转码。  
**若做过本地压测：** 只说你自己的工具、并发数、结果，别抄文档里的 200 QPS。

### Q23：Redis 用来干什么？

**答（✅）：** 仅网关 **按 IP 固定窗口限流**；Redis 不可用则**不限流**。  
📌 未做 Feed/视频详情缓存。

### Q24：监控告警有吗？

**答：** HTTP `/health` + 容器 Compose healthcheck；📌 无 Prometheus/Grafana/日志平台。

### Q25：如何部署？

`docker compose up -d --build`；依赖 MySQL、Mongo、Kafka、ES、Redis、共享卷；FFmpeg 在 worker 镜像内。

---

## 八、开放题与追问（背诵思路）

### 「和抖音差距在哪？」

分片上传、OSS、CDN 签名、AI 机审、实时推荐、亿级互动、联邦学习… 再补：**我项目把审核状态机 + 异步转码 + 发布隔离做清楚了，这是缩小差距的基础。**

### 「为什么不用单体？」

单体适合早期；转码 CPU 与 API 分离、审核与读流量分离，微服务 + 队列更易扩展。代价是分布式复杂度，用 Compose 和 internal key 降低调试成本。

### 「Mongo 丢数据怎么办？」

📌 当前单节点；生产：副本集、定期备份、写 concern majority。

### 「Kafka 挂了还能上传吗？」

能上传、能进待审；**源片通过会失败**（发不出任务）或卡在 `transcoding`；要有告警和人工重试/补偿任务。

### 「转码成功但回调失败？」

消息不 commit 会重试；可能重复转码；生产用幂等键 + 对象版本 + 对账任务。

---

## 九、代码定位速查（面试官让你「指哪打哪」）

| 话题 | 路径 |
|------|------|
| HTTP 路由 | `api-gateway/internal/router/router.go` |
| 上传 | `api-gateway/internal/handler/handler.go` → `UploadVideo` |
| 状态机 / 审核 | `video-service/internal/service/review_service.go` |
| 创建视频 | `video-service/internal/service/video_service.go` → `Create` |
| 转码回调 | 同上 → `UpdateTranscodeResult` |
| Kafka 生产 | `video-service/internal/kafka/producer.go` |
| Kafka 消费 | `transcode-worker/internal/worker/worker.go` |
| FFmpeg | `transcode-worker/internal/ffmpeg/transcoder.go` |
| gRPC 鉴权 | `pkg/auth/grpc_server.go`、`user_grpc_server.go` |
| 推荐 | `video-service/internal/service/recommend_service.go` |
| 路径安全 | `pkg/auth/pathutil.go` |
| 媒体访问 | `api-gateway/internal/handler/media.go` |

---

## 十、项目亮点（主动抛 3 条）

1. **双阶段审核 + CAS 发 Kafka**，避免重复进队；转码回调不能直发 `ready`。  
2. **发布面隔离**：`requirePublished` 统一拦住未发布视频的互动与推荐。  
3. **内外鉴权分层**：对外 JWT、对内 internal key、审核员角色 RPC。

---

## 十一、诚实短板 + 演进话术（体现思考深度）

| 短板 | 面试怎么说 |
|------|------------|
| 无分片上传 | 200MB 单次 multipart 适合 MVP；生产用 OSS Multipart + 断点续传表 |
| 无 OSS | 共享卷方便 Docker 演示；上线换 S3 兼容 API，转码 worker 从桶拉源 |
| HLS 公开 | 会加 CDN 签名 URL + 播放会话 token |
| 推荐简单 | 先规则 + 标签权重验证闭环；再上召回/排序分离 |
| 无机审 | 人工审核合规底线；机审作辅助分，接入云审核 API |
| 无限流兜底 | Redis 挂了就裸奔；应 fail-close 或本地令牌桶降级 |

---

## 十二、反问面试官（选 2 个）

1. 贵司视频链路是自建转码还是云服务？任务调度用的什么队列？  
2. 推荐系统是规则为主还是深度模型？离线特征更新周期？  
3. 后端对 Go 微服务治理（tracing、限流、灰度）有哪些规范？

---

## 十三、与旧文档的区别（避免踩坑）

仓库外另有 `week08/homework/短视频播放平台-面试问答.md`，其中 **OSS、200 QPS、LangChain 向量库** 等描述偏「目标架构」。  
**本场面试请以 `short-video-platform` 代码为准**；旧文档可当「若继续迭代」的扩展答法，不要当已实现事实陈述。

---

## 十四、考前 10 分钟 checklist

- [ ] 能白板画出：网关 → user/video gRPC → Kafka → worker → 状态机  
- [ ] 能说出 5 个视频状态及谁能看、谁能点赞  
- [ ] 能解释 internal key 防直连微服务  
- [ ] 能说明 Kafka 失败为何不 commit  
- [ ] 准备好 1 个你亲自调试过的问题（如转码路径、审核卡住、HLS 播放）  
- [ ] 对未实现项统一用「当前 MVP / 生产会」句式  

---

**相关文档：** [后端架构与流程说明.md](./后端架构与流程说明.md) · [能力对照评估.md](./能力对照评估.md)
